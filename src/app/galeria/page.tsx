import { Metadata } from "next";
import { GalleryPage } from "#app/components/pages";

export const metadata: Metadata = {
  title: "Galeria | Ence Pence Żłobek Nowa Huta",
};

const Gallery = () => {
  return <GalleryPage />;
};
export default Gallery;
